//! Public-key advertisement for Aqua services.
//!
//! Scaffolding only at this stage: the workspace split lands first so the
//! root package's test results can be compared against the pre-split
//! baseline before any new code exists.
